<h1>Welcome to the Admin Dashboard</h1>

<h2>Top Customers</h2>
<% @top_customers.each do |customer|%>
  <p><%= customer.first_name%> <%= customer.last_name %>: <%= customer.total_transactions %> transactions</p>
<% end %>

<%= link_to "Invoices", "/admin/invoices" %>
<%= link_to "Merchants", "/admin/merchants" %>

<h2>Incomplete Invoices</h2>
<% @incomplete_invoices.each do |invoice| %>
  <div id="invoice-<%= invoice.id %>">
    <%= link_to "Invoice ##{invoice.id}", "/admin/invoices/#{invoice.id}" %><br>
    <%= invoice.formatted_date %>
  </div>
<% end %>
